name: Testing e2e

on:
  pull_request:
    branches: [main]
    paths-ignore:
      - '**.md'

jobs:
  clone:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Running testing
        run: |
          make test-e2e
